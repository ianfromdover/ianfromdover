;; Copyright 2022 ianfromdover
;; github.com/ianfromdover/miryoku_kmonad
       
(defcfg
  ;; For Windows
  ;; input (low-level-hook)
  ;; output (send-event-sink)

  ;; For MacOS, replace ctl in BASE to met (the meta key, which is Command on Mac)
    ;; un-comment the 2 lines below this
  input  (iokit-name)
  output (kext)

  fallthrough true
)

(defsrc
  f j
)

(deflayer BASE
  f	(sticky-key 500 sft)
)
